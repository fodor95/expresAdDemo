{% extends 'ExpresDemoAdminBundle:Page:main.html.twig' %}


{% block main -%}

<div class="row">
        <div class="col-lg-12">
            <h1 class="page-header">Static page relationships</h1>
        </div>
        <!-- /.col-lg-12 -->
</div>
<!-- /.row -->


<form name="input" action="{{ path('static_page_relationships') }}" method="post" class="form-inline">
<table class="table">
	<thead>
		<th>Name</th>
		<th>Page name</th>
		<th>Page to route</th>
	</thead>
	<tbody>
		<tr>
			<td>Home</td>
			<td>Home</td>
			<td>
				<select name="home">
				{% for page2 in pagedb%}
					<option 
							value="{{page2.id}}"
							{% if page2.id == pages.home %}
							selected="true"
							{% endif %}
							> {{page2.title}} </option>
				{% endfor %}
				</select>
			</td>
		</tr>
		<tr>
			<td>Features</td>
			<td>alma</td>
			<td>
				<select name="features" >
				{% for page2 in pagedb%}
					<option 
							value="{{page2.id}}"
							{% if page2.id == pages.features %}
							selected="true"
							{% endif %}
					> {{page2.title}} </option>
				{% endfor %}
				</select>
			</td>
		</tr>
		<tr>
			<td>About</td>
			<td>alma</td>
			<td>
				<select name="about" >
				{% for page2 in pagedb%}
					<option 
							value="{{page2.id}}"
							{% if page2.id == pages.about %}
							selected="true"
							{% endif %}
					> {{page2.title}} </option>
				{% endfor %}
				</select>
			</td>
		</tr>
		<tr>
			<td>Contact</td>
			<td>alma</td>
			<td>
				<select name="contact" >
				{% for page2 in pagedb%}
					<option 
							value="{{page2.id}}"
							{% if page2.id == pages.contact %}
							selected="true"
							{% endif %}
					> {{page2.title}} </option>
				{% endfor %}
				</select>
			</td>
		</tr>
	</tbody>
</table>

 <input type="submit" name="submit" value="Update" class="btn btn-primary">
</form>


{% endblock %}