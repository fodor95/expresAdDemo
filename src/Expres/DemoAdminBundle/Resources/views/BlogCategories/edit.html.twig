{% extends 'ExpresDemoAdminBundle:Page:main.html.twig' %}

{% block main -%}
    <h1>BlogCategories edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('blogcategories') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
